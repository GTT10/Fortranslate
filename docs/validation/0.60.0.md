# PeleF 0.60.0 validation record

## Scope

This milestone drives sparse MPI reactive AMR topology changes directly from
solution tags, including disconnected-feature clustering, arbitrary-depth plan
construction, owner redistribution, no-op handling, and rollback.

## Automated evidence

Two separated root momentum features generate positive tags and deterministic
patch counts `[1, 2, 2, 2]` through four levels. All ranks report the same
tagged-cell count, change decision, and zero initial overlap count. Gathering
the seven rebuilt sparse owner patches reproduces every serial field, ghost,
counter, and statistic exactly.

The global sparse value count equals one replicated tree, and composite
conserved integrals remain within `2e-9`. Repeating the same tag decision is a
topology no-op that increments only the evaluation counter. An out-of-range tag
component rejects collectively with zero counts and restores the complete
sparse solution and old distribution exactly.

Branch run 32712593638 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `b726dc7`.
Branch run 32712593600 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

The public operation starts and ends with globally single-copy sparse owner
storage. Tag planning and conservative rebuild temporarily materialize a full
reactive replica on each rank. Owner-local tagging, distributed plan assembly,
point-to-point transfer, and removal of that replica remain future work.
