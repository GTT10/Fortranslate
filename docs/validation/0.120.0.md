# PeleF 0.120.0 validation record

## Scope

This milestone advances SSPRK2 molecular transport from sparse persistent MPI
reactive EB AMR state. Fine children remain owner-local through both Euler
stages, ratio subcycling, diffusive flux accumulation, reflux, and final
average-down.

## Automated evidence

The qualified 14 by 14 plane-EB root is distributed in y and has two separated
ratio-two 10 by 10 children with distinct nonuniform starting states. Direct
sparse transport must retain each rank's exact stored-value count and match
serial multipatch transport root and child state and temperature within
`2e-11` field scale at one, two, four, and eight ranks. The transport limiter
minimum must match within `2e-13`.

Owner and communicator counts must equal two root Euler updates plus twice both
children's ratio-subcycled advances. The rollback case places finite negative
density on the final child owner; root and earlier child candidates may
execute, but every local sparse field must remain bitwise unchanged, the
published Euler count must be zero, and the limiter fallback must remain one.

Implementation head `2c52d59` was qualified by ordinary CI run `32911580774`
and MPI CI run `32911580798`. GNU Fortran Release and bounds/FPE-checked Debug
both passed the complete 208-test serial regression. The MPI workflow also
passed the direct sparse transport gate and all established MPI gates with
OpenMPI at one, two, four, and eight ranks in both configurations.

## Qualification boundary

Fine payloads are direct and sparse, but root start, updated state, temperature,
and flux arrays are still level-wide temporaries. The outer sparse
`R-T-H-T-R` transaction still uses its central replicated compatibility window.
Root transport and StateRedist decomposition, targeted point-to-point traffic,
public time-loop control, dynamic topology, checkpointing, and output remain
outside this milestone.
