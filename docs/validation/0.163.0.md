# PeleF 0.163.0 validation record

## Scope

The arbitrary-depth sparse MPI tree now computes whole-tree and selected-
subtree composite conserved integrals without materialization. Every rank
recurses through the same replicated topology, while only each node owner
integrates unrefined cells. Direct-child masks exclude parent cells represented
by finer descendants before one communicator conserved-vector sum.

Distribution, sparse layout, selector, and result extent are collective
preconditions. The reduced integral and optional local-node accounting publish
only after the global result is finite and at least one owner node contributed.

## Automated evidence

After owner rotation and owner-local chemistry, the physical four-level
branching tree's sparse whole-tree integral must match the complete serial
reference within `256 epsilon` of scale and report five global nodes. Every one
of its five valid subtrees is compared with the public serial recursive
reference and must report the exact topology-derived descendant count.

For two, four, and eight ranks, rank zero selects a different but individually
valid level-three sibling from the other ranks. The one-rank gate selects an
invalid patch. Both paths must reject collectively with a zero integral and
zero public local-node count.

Qualification commit `5173400` is covered by MPI CI run `33027027060`. GNU
Fortran Debug and Release builds pass. The arbitrary-depth ownership executable
passes at OpenMPI one, two, four, and eight ranks in Release and bounds/FPE-
checked Debug configurations. The workflow also passes all established MPI
gates and the complete 208-test serial regression. Local `fparser2` parsing
passes all 206 Fortran sources.

## Qualification boundary

This milestone removes materialization from whole-tree and subtree composite
integration and supplies the conservation prerequisite for owner-local hydro.
It does not yet route recursive hydro, transport, or the public time loop
through sparse owners.
