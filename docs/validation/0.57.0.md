# PeleF 0.57.0 validation record

## Scope

This milestone advances recursive molecular transport directly on rank-local
sparse reactive AMR patches, including parabolic subcycling, diffusive flux
registers, reflux, average-down, final ghosts, bookkeeping, and rollback.

## Automated evidence

The four-level branched tree executes exactly 185 sparse owner intervals under
cumulative `r²` subcycling and records `[1, 8, 48, 128]` transport advances.
Every rank's count equals the parabolic weight of its owner-map patches. The
gathered sparse state after diffusive reflux and average-down matches serial
SSPRK2 transport within `5e-13`.

A two-level tree with six adjacent children executes exactly 25 intervals with
counters `[1, 24]`. Shared time-integrated diffusive faces cross owners, and
the gathered result matches serial within `5e-13`.

A negative density on a deepest-level owner rejects transport after earlier
intervals, reports zero committed calls, and restores every rank's sparse
fields, ghosts, and counters exactly.

Branch run 32708846959 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `8faf232`.
Branch run 32708846980 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Persistent state remains sparse. Parent states, effective diffusive fluxes,
child interiors, and compact registers use collective communication. The
combined full-physics transaction still consumes replicas. Point-to-point
communication and topology-changing distributed regrid remain future work.
