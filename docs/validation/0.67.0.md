# PeleF 0.67.0 validation record

## Scope

This milestone removes the temporary all-rank field replica from tag-driven
sparse AMR planning. Parent owners derive tags and candidate fields remain on
their assigned owners through the final direct topology transaction.

## Automated evidence

The disconnected-feature case derives a `1/2/2/2` hierarchy through four
levels. Independent traversal reproduces the communicator sums for owner tag
evaluations, candidate prolongation, final prolongation, and final overlap
messages for 1, 2, 4, and 8 ranks. The distributed hierarchy matches every
serial parent relationship and patch extent exactly.

Gathered state, temperature, narrow ghosts, wide ghosts, time, counters, and
overlap accounting match the serial tag-driven rebuild exactly. Conserved-field
installation uses a fixed temperature-recovery seed, including the case where
momentum was edited without updating cached temperature. Composite conservation
remains within `2e-9`; unchanged and invalid-tag rollback gates remain intact.
Static inspection finds neither `MPI_Bcast` nor a materialized-tree helper in
`mpi_amr_sparse_patch_1d_mod`.

Branch run 32722058407 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `04b6c7c`.
Branch run 32722058536 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Sparse 1D AMR physics, owner migration, ghost/synchronization traffic, explicit
regrid, and tag-driven regrid now avoid replicated field payloads. Compact
hierarchy metadata and correctness reductions remain replicated intentionally.
