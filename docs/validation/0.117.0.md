# PeleF 0.117.0 validation record

## Scope

This milestone removes complete hierarchy materialization from sparse MPI
reactive EB AMR chemistry. Child owners restrict conserved state and root tile
owners recover and apply the corresponding coarse temperature locally.

## Automated evidence

The qualified 14 by 14 plane-EB root is distributed in y and has two separated
ratio-two 10 by 10 children. The direct sparse chemistry path must retain each
rank's exact sparse value count. Materializing only for test observation after
commit must match serial patch-set chemistry bitwise for every root and child
state and temperature at one, two, four, and eight ranks.

The rollback case places finite negative density on the final child owner and
calls sparse average-down independently. Earlier restriction may be applied to
the candidate, but collective EOS rejection must restore every locally
allocated root tile and child field bitwise and retain its exact value count.

Implementation head `b01b373` was qualified by ordinary CI run `32906749343`
and MPI CI run `32906749351`. GNU Fortran Release and bounds/FPE-checked Debug
both passed the complete 208-test serial regression. The MPI workflow also
passed the direct sparse chemistry and average-down gate plus all established
MPI gates with OpenMPI at one, two, four, and eight ranks in both
configurations.

## Qualification boundary

Complete temporary hierarchy materialization is absent from sparse chemistry,
but each restricted coarse child footprint is still broadcast to all ranks.
Targeted point-to-point traffic, sparse hydro and transport, public time
advancement, dynamic topology, checkpointing, and output remain outside this
milestone.
