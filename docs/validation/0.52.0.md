# PeleF 0.52.0 validation record

## Scope

This milestone advances recursive patch-tree molecular transport on
deterministic MPI patch owners while preserving the serial SSPRK2 operator,
parabolic subcycling, diffusive flux ownership, reflux, and rollback rules.

## Automated evidence

`pelef_mpi_amr_patch_1d` enables viscosity, Fourier conduction,
mixture-averaged species diffusion, and barodiffusion on the four-level
branched reactive tree. Cumulative `r^2` scheduling produces exactly 185
owner patch calls and level counts `[1, 8, 48, 128]`. The complete distributed
solution agrees with serial transport within `5e-13`, transport changes the
reactive field, and composite mass, momentum, and total energy remain within
`2e-9`.

A deep density failure introduced only on the responsible owner is broadcast
before the call. The transport update is rejected collectively, returns zero
accepted local calls, and restores the complete synchronized tree exactly.

A second two-level case uses six adjacent children with cross-owner faces.
It performs exactly 25 owner updates with level counts `[1, 24]`, matches the
serial shared-diffusive-flux result within `5e-13`, and conserves within
`2e-9` after reflux and average-down.

The MPI workflow executes ownership, halo, chemistry, hydro, transport, and
rollback gates with 1, 2, 4, and 8 ranks in GNU Fortran Release and
bounds-checked Debug builds. The complete serial suite remains separate.

Branch run 32700337033 (complete MPI Debug/Release suite) passed for
implementation commit `228e210`. Branch run 32700337038 covers the complete
serial Debug/Release suite for the same commit.

## Qualification boundary

The SSPRK2 patch kernel executes on owners alone, but patch storage,
parent/child synchronization, and shared-flux/reflux work remain replicated.
The three owner physics entry points are not yet composed in one outer
distributed transaction. Sparse storage, regrid migration, and scalable
point-to-point schedules remain future work.
