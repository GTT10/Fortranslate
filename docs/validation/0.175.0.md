# PeleF 0.175.0 validation record

## Scope

The public sparse-MPI reactive 2D EB patch-tree executable reads the existing
application inputs and advances a four-level owner-local hierarchy through
recursive tagging, full physics, regridding, collective diagnostics, and
selected-root composite output.

## Automated evidence

Qualification commit `d0eafd3` is covered by ordinary CI run `33043205955`
and MPI CI run `33043205960`. GNU Fortran Debug and Release retain all 214
serial tests. MPI Debug and Release retain the complete established gate chain
and run the public application at one, two, four, and eight ranks.

The rank-count checker keys rows by level, patch, and local indices; requires
four levels, exact final time, finite positive state, seven-species closure,
identical topology and columns; and compares every numeric field within
`3e-10 * max(1, abs(one-rank reference))`. The new Release MPI parity step
passes in 17 seconds. Local `fparser2`, Python syntax, and `git diff --check`
also pass.

## Qualification boundary

This milestone qualifies fresh public MPI application runs and exposes sparse
checkpoint/restart calls. Cross-rank application restart composition and
removal of the temporary replicated root startup field remain later work.
