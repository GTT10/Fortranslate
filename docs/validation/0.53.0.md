# PeleF 0.53.0 validation record

## Scope

This milestone composes MPI owner-only chemistry, molecular transport, and
recursive hydro as one transactional patch-tree `R-T-H-T-R` interval with
complete solution-bookkeeping synchronization and outer rollback.

## Automated evidence

Before physics, the MPI gate poisons time, step, hydro and transport counters,
and regrid statistics on every non-root-owner rank. Owner synchronization
restores the root owner's complete bookkeeping exactly alongside patch fields.

The successful four-level reacting transport case executes exactly 16
chemistry calls, 33 hydro calls, and 370 transport calls globally. Every local
count equals its owner-map cumulative subcycle weight. Hydro level counters are
`[1, 4, 12, 16]`; the two transport half intervals produce
`[2, 16, 96, 256]`. Complete state, temperature, ghosts, time, step, counters,
and regrid statistics match the serial patch-tree transaction within `5e-13`.
Composite mass, momentum, and total energy remain within `2e-9`.

Omitting the transport database rejects the request without mutation. A
second rejection uses valid chemistry and transport settings but an invalid
hydro reconstruction, so the first reaction and transport operators precede
the failure. The outer transaction reports zero committed calls and restores
the complete synchronized starting solution exactly on every rank.

The MPI workflow runs the complete ownership, halo, component-operator, and
full-physics transaction gates with 1, 2, 4, and 8 ranks in GNU Fortran Release
and bounds-checked Debug builds. The complete serial suite remains separate.

Branch run 32702519543 (complete MPI Debug/Release suite) passed for
implementation commit `dec1314`. Branch run 32702519531 covers the complete
serial Debug/Release suite for the same commit.

## Qualification boundary

The outer physics transaction is distributed by patch-owner execution, but
all patch arrays and hierarchy synchronization remain replicated. Sparse
rank-local storage, regrid migration, and scalable point-to-point schedules
remain future work.
