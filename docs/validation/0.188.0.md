# PeleF 0.188.0 validation record

## Scope

Rank-deficient connected 3-by-3 cut-parent prolongation stencils grow to a
face-connected 5-by-5 neighborhood before accepting a rank-one fallback.

## Automated evidence

The focused EB AMR unit gate constructs a valid turning fluid path. Its cut
parent has only horizontal support in the compact neighborhood, while a
connected turn in the grown box supplies the second direction. An EOS-valid
two-dimensional affine conserved field must reproduce at every active child,
retain a nonzero transverse difference, and average down to the exact parent
state. The existing diagonal-plane affine, component-envelope, conservation,
temperature-recovery, EOS/PCM retry, and nonfinite rollback gates remain.

GNU Fortran Debug and Release qualification run identifiers are recorded in
the pull request before merge. The complete MPI chain is retained because the
shared dispatcher serves fixed-depth, arbitrary-depth, serial, and sparse-MPI
lifecycles. Version consistency, Fortran line length, and `git diff --check`
are also required.

## Qualification boundary

PCM remains the public default. Exact AMReX interpolation, quadratic
cut-parent reconstruction, geometry coarsening, and higher-order EB ghost
interpolation are not claimed.
