# PeleF 0.164.0 validation record

## Scope

The arbitrary-depth sparse MPI tree now advances recursive EB hydrodynamics
without materialization. Each node update remains on its owner. A distinct
child owner receives compact parent start/end exterior context, returns fine
fluxes after every recursive substep, and exchanges child state with the parent
for ordered reflux and average-down. Shared-owner edges remain local.

The operation owns one private sparse candidate. Per-node acceptance,
prevalidated owner-local subtree conservation reductions, and final sparse
validation precede one commit of fields, per-level advances, and grouped
direct-transfer counts.

## Automated evidence

After owner rotation and owner-local chemistry, the physical four-level
branching tree advances through `0.05` of its serial stable interval. Global
per-level advances must equal `[1, 2, 8, 8]`. The sum of sender transfer counts
must equal the owner-map/topology formula `parent_invocations * (r + 4)` over
all distinct-owner edges. Materialization afterward must match the complete
serial recursive hydro state and temperature within `5e-10` relative scale,
and sparse/serial composite conserved integrals must satisfy the same bound.

For two, four, and eight ranks, rank zero supplies a different but valid hydro
interval while all other ranks retain the accepted value. The one-rank gate
supplies a negative interval. Both paths must reject before mutation with exact
sparse rollback and zero hydro/transfer accounting.

Qualification commit `50069e4` is covered by MPI CI run `33028936961`. GNU
Fortran Debug and Release builds pass. The arbitrary-depth ownership executable
passes at OpenMPI one, two, four, and eight ranks in Release and bounds/FPE-
checked Debug configurations. The workflow also passes all established MPI
gates and the complete 208-test serial regression. Local `fparser2` parsing
passes all 206 Fortran sources.

## Qualification boundary

This milestone removes materialization from recursive hydrodynamics, including
parent-time context, fine-flux return, reflux, restriction, and subtree
conservation closure. Recursive owner-local SSPRK2 molecular transport and the
public sparse full-physics clock remain future work.
