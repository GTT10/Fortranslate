# PeleF 0.165.0 validation record

## Scope

The arbitrary-depth sparse MPI tree now advances recursive SSPRK2 molecular
transport without materialization. Transport fluxes, conservative RHS,
StateRedist, stage fields, temperature, and fine fluxes remain on node owners.
Compact parent context, fine-flux return, reflux, and average-down cross only
the parent/child owner boundary that needs them.

Both Euler stages, owner-local blending and EOS recovery, final deepest-first
restriction, communicator limiter reduction, and all diagnostics are one
atomic sparse transaction.

## Automated evidence

After owner-local chemistry and hydro, the physical four-level branching tree
advances through `0.02` of its serial stable interval with viscosity, thermal
conduction, species diffusion, and barodiffusion enabled. Global per-level
Euler advances must equal `[2, 4, 16, 16]`. Sender traffic must equal twice the
qualified hydro route plus one final transfer per distinct-owner relation.

Materialization afterward must match the complete serial recursive transport
state and temperature within `1e-9` relative scale. The minimum limiter must
match within `256 epsilon`, and sparse/serial composite conserved integrals
must agree within `1e-9` relative scale. Rank-dependent or negative intervals
must reject before mutation with exact sparse rollback, unit limiter, and zero
advance/transfer accounting.

Qualification commit `9b28c61` is covered by MPI CI run `33029477764`. GNU
Fortran Debug and Release builds pass. The arbitrary-depth ownership executable
passes at OpenMPI one, two, four, and eight ranks in Release and bounds/FPE-
checked Debug configurations. The workflow also passes all established MPI
gates and the complete 208-test serial regression. Local `fparser2` parsing
passes the two changed sources and the accepted 206-source tree.

## Qualification boundary

This milestone removes materialization from recursive SSPRK2 transport,
including stage context, fine-flux return, reflux, restriction, conservation
closure, stage blending, temperature recovery, and limiter reduction. The
sparse owner-local full-physics composition and public clock remain future
work.
