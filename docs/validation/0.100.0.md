# PeleF 0.100.0 validation record

## Scope

This milestone extends recursive three-level reactive EB hydrodynamics across
a finest coarse/fine interface cut by the embedded boundary. A transactional
multilevel closure restores mass, total-energy, and species conservation after
inner reflux and synchronization.

## Automated evidence

The qualified plane-EB hierarchy uses an 8 by 8 root, a ratio-two 12 by 12
middle rectangle, and a nested ratio-two 16 by 16 finest rectangle. At least
one finest boundary face has fractional open area, so the gate exercises the
cut-interface path that the preceding milestone rejected. One root interval
still executes two middle and four finest updates.

Before every middle interval, the kernel records the middle/finest composite
integral. It forms the conservative target with the signed time-integrated
middle exterior flux, measures the residual after inner reflux and
average-down, and spreads density, total-energy, and seven-species corrections
over uncovered active middle volume. The final hierarchy preserves composite
mass, total energy, and every species, retains finite positive EOS
temperatures, and is deepest-first synchronized. Existing unknown-solver
rollback retains all three original state and temperature fields.

Implementation head `bb48b13` is qualified by
[branch CI run 32864168469](https://github.com/GTT10/Fortranslate/actions/runs/32864168469)
in GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused
multipatch/multilevel gate and eleven-test public application gate passed
before the complete regression in each configuration. All 180 tests passed in
Release and Debug.

## Qualification boundary

The correction applies to density, total energy, and species. Momentum is
excluded because the slip embedded boundary supplies a physical pressure
force. Recipients are all uncovered active middle cells outside the finest
rectangle, making this a global conservative closure rather than local
PeleC-style multilevel EB redistribution. The hierarchy remains static,
serial, aligned, and exactly three levels deep. Chemistry, dynamic ownership,
checkpoint/restart, public output, transport, arbitrary depth, and MPI remain
outside this milestone.
