# PeleF 0.102.0 validation record

## Scope

This milestone exposes the qualified static three-level reactive EB kernel
through the public namelist-driven application. It adds hierarchy
construction, all-level CFL selection, final-time integration, and separate
root, middle, and finest output.

## Automated evidence

The public plane-EB case constructs an 8 by 8 root, a ratio-two 12 by 12
middle rectangle, and a nested ratio-two 16 by 16 finest rectangle. The
finest rectangle retains a two-middle-cell margin, and its interface crosses
the embedded boundary. Initialization prolongs the uniform reactive state
through both interfaces.

Each accepted root interval reduces the three active-cell stability limits
with their subcycle factors, applies the complete three-level Strang
transaction, and clips the last interval to the requested final time. The
result preserves composite mass and total energy. All three CSV files contain
their expected mesh sizes, finite positive thermodynamics, species closure,
and regular, cut, and covered cells. Active cells agree with a separately run
uniform regular-grid chemistry reference, which verifies that chemistry
actually advances.

Implementation head `b666026` is qualified by branch CI run 32868686365 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused hierarchy
and public application gates passed before the complete regression in each
configuration. All 183 tests passed in Release and Debug.

## Qualification boundary

The hierarchy is static, serial, aligned, and exactly three levels deep. The
mode intentionally rejects dynamic regridding, multipatch siblings,
checkpoint/restart, molecular transport, arbitrary-depth EB recursion, and
MPI-distributed state.
