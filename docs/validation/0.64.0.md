# PeleF 0.64.0 validation record

## Scope

This milestone replaces communicator-wide parent-state broadcasts during final
sparse ghost refresh with one direct send per distinct remote child owner.

## Automated evidence

The six-adjacent-child PPM case derives the distinct non-parent ranks that own
one or more children. Sparse chemistry reports one parent-state send per such
rank, and the communicator sum matches that count for 1, 2, 4, and 8 ranks.
The 1-rank case consumes the local parent state and reports zero sends.

Each remote owner receives one complete parent state and reuses it for all of
its local children. Existing cross-owner chemistry, recursive hydro, and
molecular-transport results retain serial field parity within `5e-13`,
composite conservation within `2e-9`, exact owner call accounting, and
transactional rollback behavior.

Branch run 32716279317 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `231a447`.
Branch run 32716279344 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Final sparse parent-state ghost refresh now reaches only relevant child owners.
Recursive hydro/transport interval states, face fluxes, level counters,
sibling flux reconciliation, topology-changing overlap transfer, and the
temporary regrid replica remain on the collective correctness schedule.
