# PeleF 0.166.0 validation record

## Scope

The arbitrary-depth sparse MPI tree now composes owner-local chemistry,
recursive SSPRK2 molecular transport, and recursive hydrodynamics as one
atomic `R-T-H-T-R` transaction. No stage materializes a complete numerical
tree. A rejected later stage leaves the accepted tree and all diagnostics
unchanged.

## Automated evidence

Starting from the qualified post-transport physical four-level tree, the gate
advances one `5e-9` full-physics interval with chemistry, viscosity, thermal
conduction, species diffusion, and barodiffusion enabled. Global chemistry
advances must equal `[2, 2, 4, 2]`, transport Euler advances
`[4, 8, 32, 32]`, and hydro advances `[1, 2, 8, 8]`. Sender traffic must equal
twice the owner-map restriction count, twice the qualified SSPRK2 transport
route, and one qualified hydro route respectively.

Materialization afterward must match the complete serial full-physics state
and temperature within `3e-9` relative scale. The transport limiter must agree
within `512 epsilon`, and sparse/serial composite integrals must agree within
`3e-9`. Rank-dependent or negative intervals must reject before mutation with
unit limiter and zero advances/transfers in all categories.

Qualification commit `6155c38` is covered by MPI CI run `33030756647`. GNU
Fortran Debug and Release builds pass. The arbitrary-depth ownership executable
passes at OpenMPI one, two, four, and eight ranks in Release and bounds/FPE-
checked Debug configurations. The workflow also passes all established MPI
gates and the complete 208-test serial regression. Local `fparser2` parsing
passes all 206 Fortran sources.

## Qualification boundary

This milestone removes materialization and partial-prefix publication from the
arbitrary-depth sparse full-physics split. The owner-local target-time clock
remains future work.
