# PeleF 0.116.0 validation record

## Scope

This milestone advances active-cell chemistry directly on rank-local sparse
MPI reactive EB AMR root tiles and sibling children. Persistent input and
output fields remain sparse across the complete transaction.

## Automated evidence

The qualified 14 by 14 plane-EB root is split into one owned y-tile per tested
rank and has two separated ratio-two 10 by 10 children. Direct sparse
chemistry must execute once on every owner entity, giving each rank its exact
distribution entity count and the communicator one call per root tile and
child. Covered cells are masked.

After owner reactions, a temporary materialization and root-owner reactive
average-down synchronize the hierarchy before immediate sparse re-scatter.
Materializing that committed sparse result must match the serial patch-set
chemistry root and child states and temperatures bitwise.

The rollback case places negative density on the final child owner. Root and
earlier child work may occur first, but every locally allocated sparse state
and temperature must return bitwise to its input and the public call count must
remain zero.

Implementation head `8bb2473` was qualified by ordinary CI run `32905551445`
and MPI CI run `32905551375`. GNU Fortran Release and bounds/FPE-checked Debug
both passed the complete 208-test serial regression. The MPI workflow also
passed the direct sparse chemistry gate and all established MPI gates with
OpenMPI at one, two, four, and eight ranks in both configurations.

## Qualification boundary

Chemistry execution is direct and sparse, but post-reaction average-down still
materializes one complete temporary hierarchy. Direct point-to-point
synchronization, sparse hydro and transport, public time advancement, dynamic
topology, checkpointing, and output remain outside this milestone.
