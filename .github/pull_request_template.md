## Purpose and scope

Issue / acceptance condition:

Supported dimensions, physics, mechanism, boundary conditions and parallel mode:

## Changes

Separate numerical changes from build/refactoring/documentation changes.
State what is deliberately unchanged.

## Evidence

Tested commit/tree (include dirty-worktree identity when applicable):
Toolchain and optional dependencies:
Commands, configuration, test regex, passed/failed/skipped counts:
Logs/artifacts:

Distinguish focused regression, full configuration matrix, installed-binary
audit, external solver parity, and physical validation. Do not substitute
historical results for this source's qualification.

## Review

- [ ] Source provenance and generated-file checks pass; no bypassed guards.
- [ ] Focused regression and relevant existing tests pass.
- [ ] Numerical tolerances/golden signatures are unchanged or justified.
- [ ] Current scope and validation evidence are updated without duplicating history.
- [ ] Remaining limitations and incomplete checks are explicit.
