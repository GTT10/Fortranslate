# PeleF 0.101.0 validation record

## Scope

This milestone composes active-cell chemistry with the static recursive
three-level reactive EB hydro kernel. Root, middle, and finest reaction
half-steps surround one hydro interval, and a final reactive deepest-first
average-down restores authoritative composite ownership.

## Automated evidence

The qualified plane-EB hierarchy uses an 8 by 8 root, a ratio-two 12 by 12
middle rectangle, and a nested ratio-two 16 by 16 finest rectangle. At least
one finest boundary face has fractional open area. The transaction applies
the generated seven-species elementary H2/O2 mechanism on every active level,
then executes one root, two middle, and four finest hydro updates before the
second reaction half-step.

The final composite state preserves mass and total energy, its seven species
sum to density, and at least one composite species integral changes. All three
temperature fields remain finite and positive, and an explicit reactive
average-down reproduces the returned root and middle state. An unknown
Riemann solver after the first chemistry half-step returns every original
state and temperature field exactly.

Implementation head `420b0b3` is qualified by branch CI run 32866345199 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused
multipatch/multilevel gate passed before the complete regression in each
configuration. All 180 tests passed in Release and Debug.

## Qualification boundary

This is a static serial kernel over exactly three aligned levels. It does not
own a public timestep/CFL loop, dynamic topology, checkpoint/restart, output,
molecular transport, arbitrary-depth recursion, locally resolved PeleC-style
multilevel EB redistribution, or MPI-distributed state.
