# PeleF 0.104.0 validation record

## Scope

This milestone adds tag-driven movement and resizing of the finest rectangle
inside the public three-level reactive EB AMR lifecycle while retaining a
fixed root-to-middle patch.

## Automated evidence

The public case constructs a 12 by 12 root and a ratio-two 20 by 20 middle
rectangle around a centered reactive hotspot. It seeds an active 8 by 8
finest rectangle, evaluates temperature-gradient tags only in the two-cell-safe
middle interior, and commits a topology replacement before time integration.
Because covered cells are not tagged, the plane EB produces a deterministic
22 by 28 finest rectangle rather than a symmetric box.

The public run must report at least one committed regrid. Root, middle, and
finest CSV files contain 144, 400, and 616 rows, respectively, reach `1e-7`,
and retain finite fields, regular/cut/covered EB classes, positive active
thermodynamics, and species closure within `8e-12`. The driver unit also
exercises an initialization-time finest replacement and validates the
resulting nested geometry and two-cell margin.

Implementation head `27b8322` is qualified by branch CI run 32874023569 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused unit and
public dynamic application gates passed before the complete regression in
each configuration. All 189 tests passed in Release and Debug.

## Qualification boundary

The root and middle topology remain fixed and the finest patch remains active.
Tags outside the interior planning region are ignored. Dynamic topology
checkpoint/restart, finest removal or siblings, dynamic parent levels,
molecular transport, arbitrary-depth EB recursion, and MPI-distributed state
remain outside this milestone.
