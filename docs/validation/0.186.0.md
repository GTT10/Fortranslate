# PeleF 0.186.0 validation record

## Scope

The conservative limited-linear reactive 2D EB AMR initializer reconstructs
active fine states across EB-cut parents while preserving each cut parent's
volume-weighted conserved average.

## Automated evidence

The diagonal-plane unit geometry carries an EOS-consistent conserved field
with variation in both directions. At least one cut parent must produce
nonconstant active fine children, and every such child component must remain
inside the active 3-by-3 coarse-neighbor envelope. The existing EB
average-down check covers the complete fine patch and must recover all coarse
states within roundoff. Regular-parent analytic reproduction, temperature
recovery, nonfinite-input rollback, and EOS-admissibility PCM retry remain in
the same test. The dynamic three-level reactive case advances the sharper
fine-level temperature field through transport, chemistry, hydro, regrid, and
reflux. Its second-order StateRedist path retries the same provisional update
with conservative order-zero redistribution if the first candidate fails EOS
recovery; unit gates cover the no-retry and transactional failed-retry paths.

Qualified head `dc7026c037d244b6f15dc25e145134e37051e135` passed GitHub
Actions CI run `33068602019` in GNU Fortran Debug and Release configurations.
MPI CI run `33068601988` passed its Debug and Release one-, two-, four-, and
eight-rank chain. Version consistency, Fortran line length, and
`git diff --check` also passed before merge.

## Qualification boundary

PCM remains the public default. Exact AMReX EB interpolation, least-squares
multidimensional gradients, higher-order cut-cell reconstruction, and
fixed-depth MPI distribution are not claimed.
