# PeleF 0.107.0 validation record

## Scope

This milestone adds transactional mixture molecular transport to the public
single-patch two-level reactive EB AMR lifecycle, including fine subcycling,
time-interpolated exterior states, diffusive reflux, and reactive average-down.

## Automated evidence

A low-level 12 by 12 root and ratio-two 16 by 16 fine-patch plane-EB hotspot
gate advances thermal conduction with level-specific transport stability
limits. It requires a reduced composite temperature span, all composite
conserved integrals within a `2e-10` scale-aware tolerance, bitwise unchanged
covered cells, an inactive limiter for the qualified conduction case, and
exact rollback for a negative interval.

The driver gate rejects a missing transport database, then accepts the same
two-level request with the matching seven-species database. It requires final
time completion, a positive accepted timestep, an active fine patch, inactive
limiting for the uniform field, and composite conservation within `8e-11`.

The public reference and transport cases advance matching 12 by 12 root and
20 by 20 fine-patch hotspots to `2e-7`. Their four CSVs require finite values,
positive active thermodynamics, unchanged EB classification, and species
closure within `8e-12`. The conducting fine-level result must have a
measurably smaller active temperature span.

Implementation head `542010d` is qualified by branch CI run 32885023049 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused AMR driver,
low-level hierarchy, public reference/run, and comparison gates passed before
the complete regression in each configuration. All 201 tests passed in
Release and Debug.

## Qualification boundary

The embedded wall remains adiabatic, free slip, and species impermeable.
Physical domain boundaries remain the established outflow subset. The fine
patch uses ratio temporal subcycling and piecewise-constant coarse exterior
data with time interpolation. Coarse-to-fine spatial slopes, thermal or
catalytic embedded walls, three-level and multipatch transport, MPI ownership,
transport checkpoint/restart, and parallel transport remain outside this
milestone.
