# PeleF 0.106.0 validation record

## Scope

This milestone adds transactional mixture molecular transport to the public
single-level reactive EB lifecycle and establishes the EB flux/divergence
foundation required by later AMR diffusive reflux.

## Automated evidence

A low-level 16 by 16 plane-EB hotspot gate advances nonzero thermal conduction
with the transport stability limit, face-centroid flux interpolation,
fluid-inventory limiting, and SSPRK2 StateRedist. It requires a reduced active
temperature span, all fluid-volume-weighted conserved integrals within an
`8e-12` scale-aware tolerance, bitwise unchanged covered cells, inactive
limiting for the qualified case, and exact rollback for a negative interval.

The public reference and transport cases advance matching 16 by 16 hotspots to
`2e-7`. Their 256-row CSVs require finite values, regular/cut/covered EB
classes, positive active thermodynamics, and species closure within `8e-12`.
The conducting result must have a measurably smaller active temperature span.
The existing driver unit additionally enables viscosity, conduction, species
diffusion, and barodiffusion on a uniform circle-EB field and requires state
preservation and composite conservation.

Implementation head `cb3c7b3` is qualified by branch CI run 32880588630 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused driver,
nonuniform conservation, public reference/run, and comparison gates passed
before the complete regression in each configuration. All 197 tests passed in
Release and Debug.

## Qualification boundary

The embedded wall is adiabatic, free slip, and species impermeable. Physical
domain boundaries remain the established outflow subset. Thermal, viscous,
catalytic, or prescribed-flux embedded walls, EB AMR diffusive reflux,
dynamic transport topology, MPI ownership, and parallel transport remain
outside this milestone.
