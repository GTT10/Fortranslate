# PeleF 0.55.0 validation record

## Scope

This milestone advances chemistry directly on rank-local sparse reactive AMR
patches and performs post-reaction average-down and ghost synchronization
without constructing a complete replicated tree.

## Automated evidence

On the four-level `1/2/3/2` tree, each rank's chemistry count equals its local
owner-map patch count and the global count is exactly eight. Child interiors
are streamed deepest-to-root for conservative average-down; parent data is
then streamed root-to-deepest for local ghost refresh. Gathering the result
matches the serial patch-tree chemistry solution within `5e-13`.

A two-level tree with six adjacent PPM children exercises sibling data crossing
owners. Normal and four-layer wide ghosts are replaced on the target owner and
the gathered sparse result matches serial chemistry within `5e-13` for 1, 2,
4, and 8 ranks.

A negative density on a deepest-level owner is encountered after earlier
patches have advanced. The failure is accepted collectively, reports zero
committed calls, and restores every rank's sparse input exactly.

Branch run 32706237139 covers the GNU Fortran Debug/Release MPI suite for
implementation commit `678f596`.
Branch run 32706237234 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Persistent patch payloads remain sparse throughout chemistry, average-down,
and ghost refresh. Communication temporarily broadcasts one patch or holds one
parent's child interiors; it is not yet the final point-to-point schedule.
Hydro, molecular transport, combined full physics, and topology-changing
distributed regrid remain future work.
