# PeleF 0.62.0 validation record

## Scope

This milestone replaces broadcast-based adjacent sparse sibling ghost refresh
with one packed bidirectional point-to-point exchange per cross-owner face.

## Automated evidence

The six-adjacent-child PPM case independently counts faces whose sibling owners
differ. Sparse chemistry reports one completed transfer per such face, and the
communicator sum matches that count for 1, 2, 4, and 8 ranks. The 1-rank case
uses only same-owner local copies and reports zero direct transfers.

Each direction packs four state/temperature boundary layers for PPM; narrow
paths pack one layer. Layer one refreshes the ordinary ghost cell and all layers
refresh the wide PPM arrays. Existing cross-owner chemistry, recursive hydro,
and molecular-transport results retain serial field parity within `5e-13` and
composite conservation within `2e-9`.

Branch run 32714651411 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `55a6e9b`.
Branch run 32714651429 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Adjacent same-level sparse ghosts now travel only between the two participating
owners. Parent interval states, child-to-parent synchronization, shared-flux
reconciliation, average-down, topology-changing overlap transfer, and the
temporary regrid replica remain on the collective correctness schedule.
