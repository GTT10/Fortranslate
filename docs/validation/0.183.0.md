# PeleF 0.183.0 validation record

## Scope

The reactive 2D EB AMR library exposes conservative MC-limited linear
coarse-to-fine initialization on topology-consistent regular parents while
retaining PCM at cut geometry and outside the EOS-admissible set.

## Automated evidence

Implementation commit `cc023c6` qualified in ordinary CI run `33059198434`.
Both GNU Fortran Debug and Release jobs completed successfully, including all
215 serial regressions. The versioned milestone remains subject to the complete
MPI qualification chain before merge.

The focused unit constructs a two-dimensional linear conserved field, checks
one analytic interior child value, and requires average-down to reproduce the
complete coarse field within roundoff. Every cut parent retains its PCM value.
A nonfinite coarse component rejects with exactly zero output state and
temperature. Version consistency and `git diff --check` pass.

## Qualification boundary

The public application and regrid planners still select PCM. Runtime method
selection, checkpoint fingerprinting of that selection, EB-aware slopes on cut
parents, and exact AMReX interpolation parity are not claimed.
