# PeleF 0.138.0 validation record

## Scope

This milestone removes the selected-root gather from sparse MPI 2D EB AMR
stable-timestep selection. Hyperbolic and molecular-transport limits are now
evaluated directly on exclusively owned root tiles and fine children before a
single communicator-minimum reduction.

## Automated evidence

Each local root tile is paired with its exact extracted EB geometry row band.
The reactive CFL and optional viscosity, thermal-conduction, and species-
diffusion limits are evaluated on that tile owner. Fine-child limits remain on
their owners and retain refinement-ratio scaling to a coarse interval. Fully
covered entities are skipped and an entirely inactive hierarchy cannot publish
the finite `huge()` sentinel as an accepted timestep.

At one, two, four, and eight ranks, the selected interval must match the serial
complete-hierarchy hydro/transport reference within `64 epsilon` while local
and communicator root-transfer counts remain exactly zero. The public clipped
time loop, committed-prefix step-limit path, and scheduled-regrid loop repeat
the zero timestep-traffic requirement. Regrid-planning traffic is checked
separately. A finite invalid child state must return zero dt and zero traffic
without changing any sparse field.

Qualification head `a09dd32` is covered by ordinary CI run `32960354417` and
MPI CI run `32960354464`. GNU Fortran Debug and Release builds pass. OpenMPI
one-, two-, four-, and eight-rank gates pass in Release and bounds/FPE-checked
Debug configurations, and both workflows pass the complete 208-test serial
regression.

## Qualification boundary

The sparse SSPRK2 transport Euler stages still gather the root to one physics
owner for diffusive flux construction and StateRedist, and the final blend
gathers both candidates to that owner. Explicit transport halo exchange and
owner-tiled transport result routing remain separate work.
