# PeleF 0.56.0 validation record

## Scope

This milestone advances recursive finite-volume hydro directly on rank-local
sparse reactive AMR patches, including subcycling, flux synchronization,
reflux, average-down, final ghosts, bookkeeping, and rollback.

## Automated evidence

The four-level `1/2/3/2` PCM tree executes exactly 33 sparse owner updates and
records `[1, 4, 12, 16]` level advances. Each rank's count equals the cumulative
subcycle weight of its owner-map patches. Gathering after streamed parent
states, effective fluxes, replicated registers, owner-local reflux, and
average-down matches serial recursive hydro within `5e-13`.

A two-level tree with six adjacent PPM children executes exactly 13 updates
with counters `[1, 12]`. Sibling ghosts and time-integrated shared-face fluxes
cross owners, and the gathered sparse result matches serial within `5e-13`.

A negative density on a deepest-level owner rejects the operation after prior
updates, reports zero committed calls, and restores every local sparse field,
ghost, time, step, and level counter exactly.

Branch run 32707936062 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `5d87995`.
Branch run 32707936011 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Patch payloads remain sparse, while interval states, effective fluxes, child
interiors, and compact flux registers use correctness-first collective
communication. Molecular transport and combined full physics still require
replicas. Point-to-point communication and topology-changing distributed
regrid remain future work.
