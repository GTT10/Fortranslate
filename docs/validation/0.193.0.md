# PeleF 0.193.0 validation record

## Scope

The public arbitrary-depth outflow-boundary cases execute active thermal
transport in serial and sparse-MPI fresh and restarted lifecycles.

## Automated evidence

The fresh four-level x-upper input enables transport with Fourier conduction
and a transport CFL of 0.35. Its existing composite checker retains exact
physical-side contact, refinement spacing, EB classes, positivity, finiteness,
and species closure. Sparse runs at one, two, four, and eight ranks must retain
complete composite field parity.

The checkpoint-stop and restart inputs use the same transport controls. Schema
4 must preserve the fingerprint, and independent serial plus cross-rank sparse
continuations must match their uninterrupted references while retaining exact
boundary contact at all four levels.

GNU Fortran Debug and Release qualification run identifiers are recorded in
the pull request before merge. Namelist consistency, version consistency, and
`git diff --check` are also required.

## Qualification boundary

The public case enables Fourier conduction alone. Viscosity, species diffusion,
and barodiffusion remain disabled in this focused boundary regression.
