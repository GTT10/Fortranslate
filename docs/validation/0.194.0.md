# PeleF 0.194.0 validation record

## Scope

The public arbitrary-depth outflow-boundary cases execute the complete
currently implemented dilute mixture-transport combination in serial and
sparse-MPI fresh and restarted lifecycles.

## Automated evidence

The fresh four-level x-upper input enables viscosity, Fourier conduction,
mixture-averaged species diffusion, and barodiffusion with a transport CFL of
0.35. The existing composite checker requires exact physical-side contact,
refinement spacing, EB classes, positivity, finiteness, and species closure.
Sparse runs at one, two, four, and eight ranks must retain complete serial
field parity.

The checkpoint-stop and restart inputs use the same transport controls. Schema
4 preserves their fingerprint, and independent serial plus cross-rank sparse
continuations must match uninterrupted references while retaining exact
boundary contact at all four levels. The sparse chain writes on two ranks and
restarts on four and eight ranks under a changed ownership weight.

GNU Fortran Debug and Release qualification run identifiers are recorded in
the pull request before merge. Namelist consistency, version consistency, and
`git diff --check` are also required.

## Qualification boundary

This gate covers the existing Newtonian/dilute mixture-averaged transport
model, including correction velocity and species enthalpy flux. It does not
establish Stefan--Maxwell, Soret, Dufour, or full PelePhysics transport parity.
