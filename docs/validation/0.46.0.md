# PeleF 0.46.0 validation record

## Scope

This milestone adds transactional runtime rebuilds and aligned overlap
transfer to arbitrary-depth reactive multipatch trees.

## Automated evidence

`regression_amr_patch_tree_reactive_1d` first reapplies its current four-level
plan and requires a no-op with only the evaluation counter changed. It then
moves both root-owned branches while retaining the `1/2/3/2` topology. The old
tree is synchronized to the root, the new tree is prolonged, and common-level
patch intersections are transferred using physical coordinates.

The accepted rebuild transfers a nonzero cell count, preserves all hydro and
transport call counters, and retains every overlapping deepest state and
temperature cell bitwise. The composite conserved state remains within
`2e-9`, every relation remains synchronized within `8e-13`, and an invalid
parent ownership plan leaves the solution and counters unchanged.

Branch CI run 32664943124 passed the full GitHub Actions Debug and Release
suite with GNU Fortran, including array-bounds checking in Debug.

## Qualification boundary

The rebuild API accepts an explicit branching plan. Automatic per-parent tag
generation and clustering, adjacent-patch same-level exchange,
physical-boundary children, load balancing, and MPI patch ownership remain
future work.
