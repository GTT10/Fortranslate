# PeleF 0.65.0 validation record

## Scope

This milestone removes the final eight broadcasts from sparse recursive hydro
and molecular transport. Interval states, child boundary fluxes, and shared-face
corrections now travel directly between their producing and consuming owners.

## Automated evidence

The six-adjacent-child PPM case reports three local communication counters:
packed parent interval-state fanout, packed child boundary-flux return, and
shared-face correction transfer. An independent hierarchy/ownership traversal
applies the exact hydro `r` and transport `r^2` invocation multipliers. The
communicator sums match all expected counts for 1, 2, 4, and 8 ranks.

Complete flux arrays and flux registers remain on their parent patch owner.
Owner-local level-counter deltas reduce once per stage and reproduce serial
hydro counters `[1, 12]` and transport counters `[1, 24]`. Existing adjacent
serial field parity within `5e-13`, composite conservation, four-level branched
call accounting, and transactional rollback gates remain unchanged. Static
source inspection also confirms that `mpi_amr_sparse_patch_1d_mod` contains no
`MPI_Bcast`.

Branch run 32718221335 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `02b4629`.
Branch run 32718221334 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Sparse physics payload communication is now owner-directed and broadcast-free.
Collective logical acceptance and counter reductions remain intentional.
Topology-changing regrid still uses a temporary all-rank correctness replica;
owner-local tagging and direct overlap transfer remain future work.
