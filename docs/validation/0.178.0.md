# PeleF 0.178.0 validation record

## Scope

Public serial and sparse-MPI arbitrary-depth EB checkpoints store and validate
a schema-2 compatibility fingerprint before accepting topology or fields.

## Automated evidence

Implementation commit `2e84dfb` is covered by ordinary CI run `33047501985`
and MPI CI run `33047584310`. GNU Fortran Debug and Release retain all 215
serial tests. MPI Debug and Release retain the complete gate chain, including
valid 2-to-4/eight-rank restart and explicit four-rank incompatible-CFL
rejection.

The serial expected-failure gate reads the same checkpoint with only CFL
changed and must publish no accepted solution. The MPI shell gate requires the
same mismatch to return nonzero before valid restarts continue. The completed
valid outputs retain identity-keyed four-level parity within
`3e-10 * max(1, abs(one-rank reference))`. The combined mismatch and valid
restart step passes in 20 seconds for Release and 58 seconds for
bounds/FPE-checked Debug. Python syntax, version consistency, and
`git diff --check` pass.

## Qualification boundary

Public application checkpoints require schema 2. Schema 1 remains available
only to low-level compatibility tests that explicitly omit a fingerprint.
