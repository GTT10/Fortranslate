# PeleF 0.108.0 validation record

## Scope

This milestone adds transactional mixture molecular transport to the public
strictly nested three-level reactive EB AMR lifecycle, including nested
subcycling, time-interpolated parent exterior states, per-interface diffusive
reflux, EB-cut closure at either interface, and deepest-first reactive
average-down.

## Automated evidence

A low-level 8 by 8 root, ratio-two 12 by 12 middle, and ratio-two 16 by 16
finest plane-EB hotspot gate advances thermal conduction under level-specific
transport stability limits. It requires a reduced temperature span, all
three-level composite conserved integrals within a `3e-10` scale-aware
tolerance, bitwise unchanged covered cells, an inactive limiter, and exact
rollback for a negative interval.

The driver gate rejects a missing transport database, then accepts the same
three-level request with the matching seven-species database. It requires
final-time completion, a positive accepted timestep, inactive limiting, and
three-level composite conservation within `2e-8`.

The public reference and transport cases advance matching 8 by 8 root,
12 by 12 middle, and 16 by 16 finest hotspots to `2e-7`. Their six CSVs
require finite values, positive active thermodynamics, unchanged EB
classification, and species closure within `8e-12`. The conducting finest
result must have a measurably smaller active temperature span.

Implementation and final branch CI evidence are recorded after qualification.

## Qualification boundary

The embedded wall remains adiabatic, free slip, and species impermeable.
Physical domain boundaries remain the established outflow subset. Both child
levels use ratio temporal subcycling and piecewise-constant parent exterior
data with time interpolation. Sibling-patch transport, coarse-to-fine spatial
slopes, thermal or catalytic embedded walls, transport checkpoint/restart,
MPI ownership, and parallel transport remain outside this milestone.
