# PeleF 0.179.0 validation record

## Scope

Multilevel 2D EB conservation closure uses only topology-derived local
coarse/fine recipient support in fixed-depth, multipatch, arbitrary-depth,
serial, and sparse-MPI paths.

## Automated evidence

Implementation commits `78a959b` and `a37861e` are exercised by ordinary CI
run `33050052551` and MPI CI run `33050052575`. GNU Fortran Debug and Release
retain all 215 serial tests. MPI Debug and Release retain the complete one-,
two-, four-, and eight-rank gate chain.

The focused multilevel unit gate constructs the mask independently and requires
nonempty active support, no refined recipient, and confinement to the clipped
three-by-three interface band. Existing fixed-depth cut-interface, sibling,
branching four-level tree, molecular-transport, chemistry, rollback, and public
application regressions retain conservation, positive thermodynamics, and
serial/MPI parity. Version consistency and `git diff --check` pass.

## Qualification boundary

The closure uses a deterministic interface-local support and uniform
per-fluid-volume correction. It does not reproduce AMReX's exact
`MLStateRedistribute` movement ledger or claim bitwise PeleC parity.
