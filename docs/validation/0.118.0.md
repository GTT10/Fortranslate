# PeleF 0.118.0 validation record

## Scope

This milestone exposes a sparse-input/output MPI reactive EB AMR
`R-T-H-T-R` transaction. Chemistry remains direct on sparse owners, while one
complete compatibility hierarchy spans the central transport and hydro work.

## Automated evidence

The qualified 14 by 14 plane-EB root is distributed in y and has two separated
ratio-two 10 by 10 children. The sparse transaction must preserve each rank's
exact stored-value count, match serial multipatch full physics for root and
child state and temperature within `5e-11` field scale, and match the transport
limiter minimum within `3e-13`.

Owner and communicator counts must equal two chemistry calls per root tile and
child, five hydro level advances, and twenty transport Euler advances. The
rollback case selects an invalid hydro solver after valid first chemistry and
transport work; every local sparse field must remain bitwise unchanged, all
published counts must be zero, and the limiter fallback must remain one.

Implementation head `94c0ac8` was qualified by ordinary CI run `32907753844`
and MPI CI run `32907753816`. GNU Fortran Release and bounds/FPE-checked Debug
both passed the complete 208-test serial regression. The MPI workflow also
passed the sparse full-physics gate and all established MPI gates with OpenMPI
at one, two, four, and eight ranks in both configurations.

## Qualification boundary

The transaction has sparse persistent input and output, but hydro and transport
share one complete temporary hierarchy. Direct sparse hydro/transport,
point-to-point synchronization, public time-loop control, dynamic topology,
checkpointing, and output remain outside this milestone.
