# PeleF 0.121.0 validation record

## Scope

This milestone removes the complete replicated hierarchy from the central
window of the sparse MPI reactive EB AMR `R-T-H-T-R` transaction. All five
physics stages now call direct sparse component entrypoints on one private
candidate.

## Automated evidence

The qualified 14 by 14 plane-EB root is distributed in y and has two separated
ratio-two 10 by 10 children. The end-to-end sparse transaction must preserve
each rank's exact stored-value count, match serial multipatch full physics for
root and child state and temperature within `5e-11` field scale, and match the
transport limiter minimum within `3e-13` at one, two, four, and eight ranks.

Owner and communicator counts must equal two chemistry calls per root tile and
child, one root plus each child's refinement-ratio hydro updates, and four root
transport Euler updates plus four times each child's refinement ratio. The
rollback case selects an invalid hydro solver after valid first chemistry and
transport work; every local sparse field must remain bitwise unchanged, all
published counts must be zero, and the limiter fallback must remain one.

Implementation head `34c743a` was qualified by ordinary CI run `32913792657`
and MPI CI run `32913792673`. GNU Fortran Release and bounds/FPE-checked Debug
both passed the complete 208-test serial regression. The MPI workflow also
passed the end-to-end sparse full-physics gate and all established MPI gates
with OpenMPI at one, two, four, and eight ranks in both configurations.

## Qualification boundary

Fine payloads stay direct and sparse across the complete physics split, but
individual hydro and transport operators still assemble level-wide root state,
temperature, and flux temporaries. Root decomposition, targeted point-to-point
traffic, public time-loop control, dynamic topology, checkpointing, and output
remain outside this milestone.
