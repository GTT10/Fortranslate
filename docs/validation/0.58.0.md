# PeleF 0.58.0 validation record

## Scope

This milestone composes chemistry, recursive hydro, and recursive molecular
transport directly on rank-local sparse reactive AMR patches as one
transactional `R-T-H-T-R` interval.

## Automated evidence

The four-level branched tree executes exactly 16 chemistry, 33 hydro, and 370
transport owner calls. Its counters finish at `[1, 4, 12, 16]` for hydro and
`[2, 16, 96, 256]` for transport, with one accepted coarse step and the exact
requested time increment.

After gathering, every sparse state, temperature, normal and wide ghost, time,
step, and counter matches the serial full-physics reference within `5e-13`.
The composite conserved integrals remain within `2e-9` of their initial values.

A required but omitted transport database is rejected before mutation. An
invalid hydro reconstruction after valid chemistry and transport prefixes is
rejected collectively; both failures report zero committed calls and restore
the complete sparse starting solution exactly.

Branch run 32709948883 passed the complete GNU Fortran Debug/Release MPI suite
with 1, 2, 4, and 8 ranks for implementation commit `10e851a`.
Branch run 32709948771 passed the complete serial Debug/Release suite for the
same implementation commit.

## Qualification boundary

Persistent full-physics state remains sparse, but component backups, streamed
interval data, and compact registers retain the correctness-first collective
schedule. Point-to-point communication and topology-changing distributed
regrid remain future work.
