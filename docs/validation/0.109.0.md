# PeleF 0.109.0 validation record

## Scope

This milestone adds transactional mixture molecular transport to the public
two-level reactive EB AMR lifecycle with separated sibling patches. The root
transport update is shared, while every child owns ratio subcycling, a
time-interpolated root exterior state, an independent diffusive flux register,
and reflux before set-wide average-down.

## Automated evidence

The low-level gate constructs a 14 by 14 plane-EB root with two separated
ratio-two 10 by 10 children. A double-hotspot conduction step requires
positive transport limits on every level, inactive species limiting, a reduced
composite temperature span, every patch-set composite conserved integral
within a `5e-10` scale-aware tolerance, bitwise unchanged covered cells, and
exact root-and-child rollback for a negative interval. The plane crosses a
child boundary, so the set-wide EB-cut conservation closure is exercised.

The public reference and conducting cases use initialization-time tag
clustering to create the same two sibling rectangles and advance to `2e-7`.
Their six CSVs require the expected 196 or 100 rows, finite values, positive
active thermodynamics, unchanged EB classification, and species closure
within `8e-12`. The conducting hierarchy must have a measurably smaller
active temperature span than the matching inert hierarchy.

Implementation head `7bcbd4f` is qualified by branch CI run 32892192076 in
GNU Fortran Release and bounds-, backtrace-, and
floating-point-exception-checked Debug configurations. The focused low-level
multipatch, public multipatch application, and molecular-transport gates
passed before the complete regression in each configuration. All 208 tests
passed in Release and Debug.

## Qualification boundary

Sibling rectangles remain separated by the established patch-set planning
margin, so same-level diffusive ghost exchange is not required. The embedded
wall remains adiabatic, free slip, and species impermeable. Physical domain
boundaries remain the established outflow subset. Transport
checkpoint/restart, coarse-to-fine spatial slopes, thermal or catalytic
embedded walls, distributed EB AMR ownership, and parallel transport remain
outside this milestone.
