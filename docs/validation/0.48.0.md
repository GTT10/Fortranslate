# PeleF 0.48.0 validation record

## Scope

This milestone adds same-level ghost exchange and single fine/fine interface
ownership for independently stored adjacent patch-tree children.

## Automated evidence

`unit_amr_patch_tree_1d` constructs two adjacent children and proves that the
patch-tree opt-in accepts them while the pre-existing separated patch-set gate
continues to reject adjacency by default.

`regression_amr_patch_tree_reactive_1d` constructs two ratio-two children over
adjacent parent intervals. Before advancement, the two face ghosts and every
state and temperature value in the four-layer PPM/WENO ghost storage match the
opposite sibling interior bitwise. A PPM hydro interval produces exact `1/4`
root/fine call counts, preserves every composite conserved component within
`3e-10`, and remains synchronized within `5e-13`. Ghost equality is checked
again after the accepted step.

A separate reactive-hotspot state enables viscosity, conduction, species
diffusion, and barodiffusion. Its transport--hydro--transport interval produces
exact `2/16` parabolic call counts, preserves the composite state within
`2e-9`, remains synchronized within `8e-13`, and finishes with exact adjacent
ghost exchange. Internal fine/fine register sides are suppressed before
reflux in both paths.

Branch CI run 32692406244 passed the complete GitHub Actions Debug and Release
suite with GNU Fortran, including array-bounds checking in Debug.

## Qualification boundary

The exchange is parent-local and same-process for strictly interior children.
Periodic-seam splitting, physical-boundary children, load balancing,
stage-synchronous distributed exchange, and MPI patch ownership remain future
work.
