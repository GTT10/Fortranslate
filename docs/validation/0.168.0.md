# PeleF 0.168.0 validation record

## Scope

The serial arbitrary-depth reactive 2D EB tree now plans branching topology
from accepted temperature fields. It synchronizes deepest-first, tags and
clusters every prospective parent independently, asks a caller callback for EB
child geometry, and composes the complete plan with the established atomic
overlap-preserving rebuild.

## Automated evidence

An eight-by-eight root-only EB tree contains one thermodynamically consistent
hot cell. The planner must create one child at each of two relations, preserve
the exact parent association, and stop at the requested three-level ceiling.
The public tagged-regrid operation must produce a valid `1/1/1` tree and
preserve the complete composite conserved vector within `8e-12` of its scale.

Repeating the same plan must be a field-exact no-op. A geometry callback that
rejects the first child must leave the accepted tree exact and publish no
topology change. After every node is made uniform, the planner must report zero
tags, collapse the topology to its root, and again preserve the composite
integral within `8e-12`.

Qualification commit `76d20b0` is covered by ordinary CI run `33033548506`.
GNU Fortran Debug and Release builds pass the dedicated EB multilevel gate and
the complete 208-test serial regression. Local `fparser2` parsing passes both
changed Fortran sources.

## Qualification boundary

This milestone closes solution-driven serial topology planning for the
arbitrary-depth numerical EB tree. Owner-local MPI tag planning and direct
topology-changing migration, checkpoint/restart, and composite output remain
future lifecycle work.
