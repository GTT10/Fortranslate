# PeleF 0.200.0 validation record

## Scope

The public arbitrary-depth serial and sparse-MPI full-physics lifecycle retains
a branching four-level tree through fresh execution and independent restart.

## Automated evidence

The boundary case initializes separated x-upper and interior temperature
features. Its checker requires four populated levels, x-upper contact on every
level, complete EB cell classes, positive thermodynamic fields, species
closure, and at least two patch identities on one populated level.

The restart checker applies the same branching and boundary requirements to
the uninterrupted, stopped, and continued outputs. It retains complete
identity-keyed field comparison, cumulative diagnostics, and schema-8 envelope
checks. Sparse MPI runs compare one, two, four, and eight fresh ranks and a
two-rank checkpoint continued at four and eight ranks under changed ownership.
GNU Fortran Debug and Release CI run identifiers are recorded in the pull
request.

## Qualification boundary

This milestone qualifies ordered sibling branches with one outflow-boundary
branch and one interior branch. Periodic-seam children and non-outflow refined
physical boundaries remain separate work.
