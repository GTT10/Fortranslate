# PeleF 0.182.0 validation record

## Scope

Public fixed-depth, arbitrary-depth, serial, and sparse-MPI EB AMR applications
apply the embedded-wall values from `&embedded_boundary`. Their checkpoint and
restart contracts identify the wall and all molecular-transport controls.

## Automated evidence

The implementation content first qualified in ordinary CI run `33056186291`
and MPI CI run `33056186255`. The rebased PR head `0726c06` then qualified in
ordinary CI run `33056638159` and MPI CI run `33056638130`. Every GNU Fortran
Debug and Release job completed successfully, including all 215 serial
regressions in MPI and non-MPI builds.

The focused two-level gate advances an active 1500 K isothermal wall, writes a
transport checkpoint, restarts it at the recorded final state, and rejects a
10 K wall-temperature mismatch with a neutral clock. The public two-level AMR
transport case also reads a 1500 K moving no-slip wall from the namelist and
requires measurable cut-cell heating and tangential momentum transfer. The
arbitrary-depth fingerprint gate verifies that the same temperature change
fails matching. Version consistency and `git diff --check` pass.

## Qualification boundary

Schema-1 fixed-depth and schema-2 fingerprinted arbitrary-depth checkpoints are
not migrated; the new readers reject them. Wall species remain impermeable.
Catalytic conversion, higher-order wall-normal stencils, and external PeleC
checkpoint compatibility are not claimed.
