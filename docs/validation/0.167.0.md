# PeleF 0.167.0 validation record

## Scope

The arbitrary-depth sparse MPI tree now owns a public target-time clock. It
recomputes the owner-local hydro/transport stability interval before every
attempted step, clips the final interval, advances one private owner-local
`R-T-H-T-R` candidate, and commits state, clock, minima, work, and traffic
diagnostics together.

## Automated evidence

Starting from the qualified post-full-physics four-level tree at time zero, the
serial and sparse clocks target `2.5e-9` and must both take one clipped step.
The accepted time must equal the target exactly, the minimum dt must equal the
serial result, and the global timestep-node evaluation count must equal five.

Chemistry, transport, and hydro advances and transfers must equal the exact
one-step schedules qualified in `0.166.0`. Materialized state and temperature
must match the serial clock within `6e-9`, the limiter within `1024 epsilon`,
and composite conserved integrals within `6e-9`. Rank-dependent or negative
target times and a zero maximum-step limit must preserve the sparse tree and
clock exactly with zero diagnostics and neutral minima.

Qualification commit `e005621` is covered by MPI CI run `33031856954`. GNU
Fortran Debug and Release builds pass. The arbitrary-depth ownership executable
passes at OpenMPI one, two, four, and eight ranks in Release and bounds/FPE-
checked Debug configurations. The workflow also passes all established MPI
gates and the complete 208-test serial regression. Local `fparser2` parsing
passes all 206 Fortran sources.

## Qualification boundary

This milestone completes the owner-local timestep/full-physics target-time path
for the sparse arbitrary-depth EB tree. Dynamic tagging, checkpoint/restart,
and output remain future lifecycle work.
